import React from "react";
import Layout from "../../base/Layout";
import { Text } from "react-native";

const GameScore = () => {
	return (
		<Layout>
			<Text>Page du score</Text>
		</Layout>
	);
};

export default GameScore;
