import React from "react";
import Layout from "../../base/Layout";
import { Text } from "react-native";

const Game = () => {
	return (
		<Layout>
			<Text>Game</Text>
		</Layout>
	);
};

export default Game;
